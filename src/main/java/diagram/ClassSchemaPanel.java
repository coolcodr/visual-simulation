package diagram;
import java.awt.event.*;
import javax.swing.*;
import java.awt.*;
import java.util.*;

/**
 * Title:
 * Description:
 * Copyright:    Copyright (c) 2001
 * Company:
 * @author
 * @version 1.0
 */

/*
public class ClassSchemaPanel extends JPanel {
	
	private TextEditor _textEditor;
	
	private Vector _properties;
	
	privaye JTextField _pName;
	privaye JComboBox _pType;
	
	private JButton _addButton;
	private JButton _removeButton;
	private JButton _submitButton;
	
	public ClassSchemaPanel(TextEditor textEditor) {
		super();
		
		_textEditor = textEditor;
		
		_addButton = new JButton("Add");
		_removeButton = new JButton("Remove");
		_submitButton = new JButton("Submit");
		
		_pName = new JTextField();
		_pType = new JComboBox();
		_pType.addItem("int");
		_pType.addItem("double");
		_pType.addItem("String");
		
		JLabel pName = new JLabel("Name");
		JLabel pType = new JLabel("Type");
		
		add(pName);
		add(_pName);
		add(pType);
		add(_pType);
		
		add(_addButton);
		add(_removeButton);
		add(_submitButton);
	}
}
*/